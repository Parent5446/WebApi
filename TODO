* Check for columns in database for DB_Object to check for valid profile data.
* Put in commands to use the Logger in appropriate places.
* Create easy-to-use functions that might be helpful in writing controllers.
* Let database objects have preconfigured columns, and be able to set up their own table.
* Create authorization between classes.
	--Groups stored in database column of object.
	--Permissions for group configured in config file.
	--Object gives $this to the other object for auth check.
